{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the controller and projection windows",
  "windows": ["main", "projection"],
  "permissions": ["core:default"]
}
